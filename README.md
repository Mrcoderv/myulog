# ULog
ULog is an initiative to standardize and make actionable the telemetry generated by AI systems. By introducing versioned JSON logging contracts, a normalization and enrichment pipeline, and a deterministic classification engine, ULog ensures that heterogeneous logs from APIs, LLMs, agents, and CV pipelines become reliable, auditable signals. 

## Schemas
### LLM Interaction Log (schemas/llm.schema.json)

The LLM Interaction Contract Schema defines the JSON structure for logging interactions with a large language model (LLM). 

#### Schema Overview 
| Field                            | Type               | Description                                                                                                   | Required    |
| -------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------- | ----------- |
| log_id                           | string (uuid)      | Unique identifier for the log entry.                                                                          | Yes         |
| log_timestamp                    | string (date-time) | Timestamp when the log entry was created.                                                                     | Yes         |
| outcome                          | string             | Operation result: `success` or `failure`.                                                                     | Yes         |
| model                            | string             | Name of the LLM model used.                                                                                   | Yes         |
| category                         | string             | Event domain (e.g., `auth`, `network`, `data`, `model`, etc.).                                                | Yes         |
| prompt                           | object             | Details of the prompt sent to the LLM.                                                                        | Yes         |
| prompt.prompt_id                 | string (uuid)      | Unique identifier for the prompt.                                                                             | Yes         |
| prompt.input_tokens              | integer            | Number of tokens in the prompt.                                                                               | Yes         |
| prompt.prompt_text               | string             | Text content of the prompt.                                                                                   | Yes         |
| prompt.parameters                | object             | Optional model parameters: temperature, top_p, max_tokens, stop_sequences.                                    | No          |
| prompt.parameters.temperature    | number             | Temperature parameter for sampling (0–2).                                                                     | No          |
| prompt.parameters.top_p          | number             | Cumulative probability threshold for sampling (0–1).                                                          | No          |
| prompt.parameters.max_tokens     | integer            | Maximum tokens the model can generate.                                                                        | No          |
| prompt.parameters.stop_sequences | array[string]      | Custom stop sequences to end generation.                                                                      | No          |
| result                           | object             | Details of the model’s response.                                                                              | Yes         |
| result.result_id                 | string (uuid)      | Unique identifier for the response.                                                                           | Yes         |
| result.response_text             | string             | Full text generated by the LLM.                                                                               | Yes         |
| result.output_tokens             | integer            | Number of output tokens generated.                                                                            | Yes         |
| result.anonymization_note        | string             | Notes on any sensitive data that has been anonymized.                                                         | No          |
| result.latency_ms                | integer            | Total latency of the request in milliseconds.                                                                 | Yes         |
| result.model_quality_flags       | string             | Model quality issues detected in the output.                                                                  | No          |
| result.safety_flags              | string             | Sensitive/safety flags (`llm`, `cv`, `pii`, `security`).                                                      | No          |
| metadata                         | object             | Optional metadata about the log entry.                                                                        | No          |
| metadata.source                  | string             | Optional source of the log entry.                                                                             | No          |
| metadata.ingestion_ts            | string (date-time) | Timestamp when the log entry was ingested.                                                                    | No          |
| metadata.contract_version        | string             | Version of the schema contract (SemVer).                                                                      | No          |
| metadata.pipeline_stage          | string             | Stage of the pipeline (`ingestion`, `preprocessing`, `retrieval`, `generation`, `postprocessing`, `storage`). | No          |
| component                        | string             | Responsible component (`api`, `agent`, `pipeline`).                                                           | No          |
| level                            | string             | Log severity (`info`, `warn`, `error`).                                                                       | No          |
| usage                            | object             | Resource usage metrics.                                                                                       | No          |
| usage.prompt_tokens              | integer            | Number of tokens in the prompt.                                                                               | No          |
| usage.completion_tokens          | integer            | Number of tokens in the generated response.                                                                   | No          |
| usage.total_tokens               | integer            | Total tokens used (prompt + completion).                                                                      | No          |
| sampler                          | object             | Sampling parameters used.                                                                                     | No          |
| sampler.temperature              | number             | Temperature parameter used.                                                                                   | No          |
| sampler.top_p                    | number             | Top-p parameter used.                                                                                         | No          |
| sampler.top_k                    | integer            | Top-k parameter used.                                                                                         | No          |
| sampler.presence_penalty         | number             | Penalty for new token presence (-2 to 2).                                                                     | No          |
| sampler.frequency_penalty        | number             | Penalty for token frequency (-2 to 2).                                                                        | No          |
| sampler.stop                     | string             | Stop sequence used for generation.                                                                            | No          |
| sampler.seed                     | integer            | Optional seed for deterministic output.                                                                       | No          |
| rag                              | object             | Retrieval-Augmented Generation details.                                                                       | No          |
| rag.query                        | string             | Query used for retrieval.                                                                                     | No          |
| rag.citations                    | string             | Citations or sources retrieved.                                                                               | No          |
| rag.stage                        | string             | Stage of RAG (`query`, `retrieval`, `generation`).                                                            | No          |
| metrics                          | object             | Aggregated performance metrics.                                                                               | No          |
| metrics.average_latency_ms       | number             | Average latency in milliseconds.                                                                              | No          |
| metrics.success_rate             | number             | Success rate (0–1).                                                                                           | No          |
| metrics.accuracy                 | number             | Accuracy (0–1).                                                                                               | No          |
| error_log                        | array of objects   | Errors encountered during prompting or response generation.                                                   | No          |
| error_log.error_code             | string             | Stable short codes for known error types.                                                                     | Conditional |
| error_log.error_type             | string             | Type of the error.                                                                                            | Conditional |
| error_log.message                | string             | Detailed error message.                                                                                       | Conditional |
| error_log.timestamp              | string (date-time) | Timestamp when the error occurred.                                                                            | Conditional |


**Note:** `error_log` is not required but, if present, each entry should include error_type, message, and timestamp.

###  PII & Anonymization Policy

Logs must **not** include personally identifiable information (PII) or sensitive text content.  
If a prompt or response might contain private data:

- **Truncate** long text to a short snippet.  
- **Redact** or mask names, emails, IDs, or confidential values.  
- Optionally include a small note field such as  
  `"anonymization_note": "truncated 200 chars"` to record that redaction was applied.

#### Example — Privacy-safe log entry
```json
{
  "log_id": "11111111-1111-4111-8111-111111111111",
  "log_timestamp": "2025-10-08T09:00:00Z",
  "model": "gpt-5-mini",
  "outcome": "success",
  "category": "model",
  "level": "info",
  "prompt": {
    "prompt_id": "aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa",
    "input_tokens": 45,
    "prompt_text": "Summarize the following paragraph… (truncated)"
  },
  "result": {
    "result_id": "bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb",
    "response_text": "The summary of the paragraph is… (truncated)",
    "anonymization_note": "truncated 200 chars",
    "output_tokens": 120,
    "latency_ms": 250
  }
}
```

### UUID Policy
- Use Version 4 UUID for all IDs (`log_id`, `prompt_id`, `result_id`)
- Generate the ID randomly at the time when the log is created. 
- All IDs must be globally unique across all logs. 
- The IDs are all lowercase strings with hyphens. 