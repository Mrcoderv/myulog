[project]
name = "ulog"
version = "0.1.0"
description = ""
authors = [
    {name = "RakibCSE",email = "rakibul.hossain365@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jsonschema (>=4.25.1,<5.0.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "jsonschema (>=4.25.1,<5.0.0)",
    "pytest (>=8.4.2,<9.0.0)",
    "ruff (>=0.14.0,<0.15.0)",
    "mypy (>=1.18.2,<2.0.0)",
    "pytest-cov (>=7.0.0,<8.0.0)",
    "types-jsonschema (>=4.25.1.20251008,<5.0.0.0)"
]
